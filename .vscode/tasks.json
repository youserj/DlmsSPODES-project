{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "install-editable",
            "type": "shell",
            "command": "py -3.12 -m pip install -e .",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}